<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./head.ejs"); %>
    <title>CutUrls | <%- id %></title>
</head>
<body>
    <h1>Redirecting you to <code><%- redirectUrl %></code> in <span id="seconds"><%- seconds %></span> Seconds </h1>
    <h2>This shortURL has been clicked <%- clicks %> times.</h2>
    <h2><a href="<%- redirectUrl %>">
        Click here
    </a>if you weren't automatically redirected.</h2>
    <script>
        let seconds = "<%- seconds %>";
        const countdown = () => {
            seconds = seconds - 1;
            if (seconds < 0) {
                window.location.replace("<%- redirectUrl %>");
            } else {
                document.getElementById("seconds").innerText = seconds;
                window.setTimeout("countdown()", 1000);
            }
        }
        countdown();
    </script>
    <%- include ("./footer.ejs") %>
</body>
</html>